<h1>Tweets#confirm</h1>
<%= form_for(@tweet) do |f| %>
    <%= f.hidden_field :content %>
    <p>投稿内容：<%= @tweet.content %></p>
    <%= f.submit "投稿" %>
<% end %>

<%= form_for(@tweet, url: tweets_path, method: "get" ) do |f| %>
    <%= f.hidden_field :content %>
    <%= f.submit "戻る", name: "back" %>
<% end %>